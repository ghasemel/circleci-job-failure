version: 2.1

jobs:
  myjob:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: return 1
      - run:
          command: echo "Hello Error!"
          when: on_fail      

workflows:
  my_workflow:
    jobs:
      - myjob
